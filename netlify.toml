[build]
command = "npm run build"
publish = "public"

[build.environment]
HUGO_VERSION = "0.104.2"

[functions]
directory = "netlify/functions/"
node_bundler = "esbuild"

[dev]
command = "npm run dev"
framework = "hugo"
publish = "public"
